#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplies two integers.
 * @a: first integer argument
 * @b: second integer argument
 * Description: prints the result of multiplication to the standard output.
 * Return: 0 Success
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
